# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Augmenté à 4096m pour éviter le blocage infini à 76% (compileDebugKotlin)
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true

# AndroidX package structure
android.useAndroidX=true

# Architectures à compiler
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Activer la Nouvelle Architecture (TurboModules & Fabric)
newArchEnabled=true

# Activer le moteur JavaScript Hermes
hermesEnabled=true

# Support d'affichage plein écran (Edge-to-edge)
edgeToEdgeEnabled=false

# Configuration du cache pour la stabilité du build
org.gradle.daemon=true
org.gradle.caching=true